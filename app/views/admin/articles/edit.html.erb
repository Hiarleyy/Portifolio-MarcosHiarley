<div class="admin-page-header">
  <div>
    <h1>Editar Artigo</h1>
    <p>Editando: <%= @article.title %></p>
  </div>
  <a href="<%= admin_articles_path %>" class="btn btn-outline btn-sm">
    <i class="ph ph-arrow-left"></i> Voltar
  </a>
</div>

<%= render 'form', article: @article %>
