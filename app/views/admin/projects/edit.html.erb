<div class="admin-page-header">
  <div>
    <h1>Editar Projeto</h1>
    <p>Editando: <%= @project.title %></p>
  </div>
  <a href="<%= admin_projects_path %>" class="btn btn-outline btn-sm">
    <i class="ph ph-arrow-left"></i> Voltar
  </a>
</div>

<%= render 'form', project: @project %>
